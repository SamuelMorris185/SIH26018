# Docker Compose only. Application settings live in backend/.env and frontend/.env.
# DEVELOPMENT/DEMO ONLY. Match backend DATABASE_URL if changing these values.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=sih26018_db
POSTGRES_PORT=5432
